import { createTheme } from "@mui/material";

const Theme = createTheme({
  palette: {
    primary: {
      main: "#e13639",
      contrastText: "#2d2d2c",
    },
    secondary: {
      main: "#e13639",
      contrastText: "#2d2d2c",
    },
    background: {
      default: "#ffffff",
      paper: "#fffffa",
    },
  },
  
});

export default Theme;
